# auto-generated by grc.converter

id: lte_remove_cp_cvc
label: Remove CP
category: lte

parameters:
-   id: fftl
    label: FFT length
    dtype: int
    hide: ${ 'part' if vlen == 1 else 'none' }
-   id: key
    label: tag key value
    dtype: string

inputs:
-   domain: stream
    dtype: complex

outputs:
-   domain: stream
    dtype: complex
    vlen: ${ fftl }

templates:
    imports: import lte
    make: lte.remove_cp_cvc(${fftl}, ${key}, "$id")

file_format: 1
