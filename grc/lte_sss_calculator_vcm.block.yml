# auto-generated by grc.converter

id: lte_sss_calculator_vcm
label: SSS Calculator
category: lte

parameters:
-   id: fftl
    label: FFT length
    dtype: int
-   id: in_key
    label: cell ID group key
    dtype: string
-   id: out_key
    label: offset marker key
    dtype: string

inputs:
-   domain: stream
    dtype: complex
    vlen: 72

outputs:
-   domain: message
    id: cell_id
-   domain: message
    id: frame_start

templates:
    imports: import lte
    make: lte.sss_calculator_vcm(${fftl}, ${in_key}, ${out_key}, "$id")

file_format: 1
