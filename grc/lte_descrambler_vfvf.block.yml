# auto-generated by grc.converter

id: lte_descrambler_vfvf
label: LTE Channel Descrambler
category: lte

parameters:
-   id: tag_key
    label: Tag Key
    dtype: string
-   id: vlen
    label: vector length
    dtype: int
    hide: ${ 'part' if vlen == 1 else 'none' }
-   id: initial_seqs
    label: Initial sequences
    dtype: raw

inputs:
-   domain: stream
    dtype: float
    vlen: ${ vlen }
-   domain: message
    id: seqs

outputs:
-   domain: stream
    dtype: float
    vlen: ${ vlen }

templates:
    imports: import lte
    make: |-
        lte.descrambler_vfvf(${tag_key}, "seqs", ${vlen}, "$id")
        self.${id}.set_descr_seqs(${initial_seqs})

file_format: 1
