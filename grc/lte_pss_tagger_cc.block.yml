# auto-generated by grc.converter

id: lte_pss_tagger_cc
label: PSS Frame Tagger
category: lte

parameters:
-   id: fftl
    label: FFT length
    dtype: int

inputs:
-   domain: stream
    dtype: complex
-   domain: message
    id: lock
-   domain: message
    id: half_frame
-   domain: message
    id: N_id_2

outputs:
-   domain: stream
    dtype: complex

templates:
    imports: import lte
    make: lte.pss_tagger_cc(${fftl}, "$id")

file_format: 1
