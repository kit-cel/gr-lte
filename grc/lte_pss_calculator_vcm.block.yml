# auto-generated by grc.converter

id: lte_pss_calculator_vcm
label: PSS Calculator
category: lte

parameters:
-   id: fftl
    label: FFT length
    dtype: int

inputs:
-   domain: stream
    dtype: complex
    vlen: 72

outputs:
-   domain: message
    id: lock
-   domain: message
    id: half_frame
-   domain: message
    id: N_id_2

templates:
    imports: import lte
    make: lte.pss_calculator_vcm(${fftl}, "$id")

file_format: 1
