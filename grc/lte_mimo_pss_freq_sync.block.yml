# auto-generated by grc.converter

id: lte_mimo_pss_freq_sync
label: MIMO PSS freq sync
category: lte

parameters:
-   id: fftl
    label: FFT length
    dtype: int
-   id: rxant
    label: RX antennas
    dtype: int
    hide: part
-   id: sig
    label: Signal Source
    dtype: raw

inputs:
-   domain: stream
    dtype: complex
    multiplicity: ${ rxant }

templates:
    imports: import lte
    make: lte.mimo_pss_freq_sync(${fftl},${rxant},self.${sig})

file_format: 1
