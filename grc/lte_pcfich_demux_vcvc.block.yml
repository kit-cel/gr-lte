# auto-generated by grc.converter

id: lte_pcfich_demux_vcvc
label: PCFICH Demux
category: lte

parameters:
-   id: N_rb_dl
    label: resource blocks
    dtype: int
    hide: ${ 'part' if vlen == 1 else 'none' }
-   id: key
    label: Input key
    dtype: string
-   id: out_key
    label: Output key
    dtype: string

inputs:
-   domain: stream
    dtype: complex
    vlen: ${ 12 * N_rb_dl }

outputs:
-   domain: stream
    dtype: complex
    vlen: 16

templates:
    imports: import lte
    make: lte.pcfich_demux_vcvc(${N_rb_dl}, ${key}, ${out_key}, "cell_id", "$id")

file_format: 1
