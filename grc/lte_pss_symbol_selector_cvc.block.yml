# auto-generated by grc.converter

id: lte_pss_symbol_selector_cvc
label: PSS Symbol Selector
category: lte

parameters:
-   id: fftl
    label: FFT length
    dtype: int
    hide: ${ 'part' if vlen == 1 else 'none' }

inputs:
-   domain: stream
    dtype: complex
-   domain: message
    id: lock
-   domain: message
    id: half_frame

outputs:
-   domain: stream
    dtype: complex
    vlen: ${ fftl }

templates:
    imports: import lte
    make: lte.pss_symbol_selector_cvc(${fftl}, "$id")

file_format: 1
