# auto-generated by grc.converter

id: lte_mib_unpack_vbm
label: Unpack MIB
category: lte

inputs:
-   label: mib
    domain: stream
    dtype: byte
    vlen: 24
-   label: N_ant
    domain: stream
    dtype: byte
    vlen: 1

outputs:
-   domain: message
    id: SFN
    optional: true
-   domain: message
    id: N_ant
    optional: true
-   domain: message
    id: N_rb_dl
    optional: true
-   domain: message
    id: phich_duration
    optional: true
-   domain: message
    id: phich_resources
    optional: true

templates:
    imports: import lte
    make: lte.mib_unpack_vbm("$id")

file_format: 1
