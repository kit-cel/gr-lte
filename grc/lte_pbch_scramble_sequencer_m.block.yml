# auto-generated by grc.converter

id: lte_pbch_scramble_sequencer_m
label: Scrambling sequence message
category: lte

inputs:
-   domain: message
    id: cell_id

outputs:
-   domain: message
    id: vector

templates:
    imports: import lte
    make: lte.pbch_scramble_sequencer_m("$id")

file_format: 1
