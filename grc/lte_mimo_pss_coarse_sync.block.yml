# auto-generated by grc.converter

id: lte_mimo_pss_coarse_sync
label: MIMO PSS coarse sync
category: lte

parameters:
-   id: syncl
    label: Sync length
    dtype: int
-   id: rxant
    label: RX antennas
    dtype: int

inputs:
-   domain: stream
    dtype: complex
    multiplicity: rxant

outputs:
-   domain: message
    id: N_id_2
    optional: true
-   domain: message
    id: coarse_pos
    optional: true
-   domain: message
    id: control
    optional: true

templates:
    imports: import lte
    make: lte.mimo_pss_coarse_sync(${syncl}, ${rxant})

file_format: 1
