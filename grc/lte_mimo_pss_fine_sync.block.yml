# auto-generated by grc.converter

id: lte_mimo_pss_fine_sync
label: MIMO PSS fine sync
category: lte

parameters:
-   id: fftl
    label: FFT length
    dtype: int
-   id: rxant
    label: RX antennas
    dtype: int
    hide: part
-   id: grpdelay
    label: Filter Group Delay
    dtype: int

inputs:
-   domain: stream
    dtype: complex
    multiplicity: ${ rxant }
-   domain: message
    id: N_id_2
-   domain: message
    id: coarse_pos

outputs:
-   domain: message
    id: half_frame
-   domain: message
    id: lock

templates:
    imports: import lte
    make: lte.mimo_pss_fine_sync(${fftl}, ${rxant}, ${grpdelay})

file_format: 1
