<?xml version="1.0"?>
<block>
  <name>Descrambler</name>
  <key>lte_descrambler_vfvf</key>
  <category>lte</category>
  <import>import lte</import>
  <make>lte.descrambler_vfvf($tag_key, "seqs", $len)</make>
  <!-- Make one 'param' node for every Parameter you want settable from the GUI.
       Sub-nodes:
       * name
       * key (makes the value accessible as $keyname, e.g. in the make node)
       * type -->
  <param>
    <name>tag key value</name>
    <key>tag_key</key>
    <type>string</type>
  </param>

  <param>
    <name>vector length</name>
    <key>len</key>
    <type>int</type>
  </param>

  <!-- Make one 'sink' node per input. Sub-nodes:
       * name (an identifier for the GUI)
       * type
       * vlen
       * optional (set to 1 for optional inputs) -->

  <sink>
    <name>seqs</name>
    <type>message</type>
    <optional>1</optional>
  </sink>
  <sink>
    <name>in</name>
    <type>float</type>
    <vlen>$len</vlen>
  </sink>

  <!-- Make one 'source' node per output. Sub-nodes:
       * name (an identifier for the GUI)
       * type
       * vlen
       * optional (set to 1 for optional inputs) -->
  <source>
    <name>out</name>
    <type>float</type>
    <vlen>$len</vlen>
  </source>
</block>
